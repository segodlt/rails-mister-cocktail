<h1>Add a ingredient to <%= @cocktail.name %></h1>

<div class="container">
  <%= simple_form_for [@cocktail, @dose] do |f|%>
    <%= f.input :description %>
    <%= f.association :ingredient, collection: Ingredient.order('name ASC') %>
    <%= f.submit %>
  <% end %>
</div>
