<div class="card-product">
  <img src="https://images.unsplash.com/photo-1498772776855-2248a3e740f3?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=826&q=80" />
  <div class="card-product-infos">
    <h2><%= @cocktail.name %></h2>
    <% @cocktail.doses.each do |dose| %>
      <p><%= dose.description %> de : <%= dose.ingredient.name %> <%= link_to "🗑", dose_path(dose), method: :delete, data: { confirm: "Are you sure?"} %></p>
    <% end %>
    <%= link_to new_cocktail_dose_path(@cocktail.id) do %>
      <i class="fas fa-cart-plus" ></i>
    <% end %>
  </div>
</div>





<%= link_to new_cocktail_review_path(@cocktail.id) do %>
  <i class="fas fa-chevron-circle-left"></i>
<% end %>

<%= link_to cocktails_path do %>
  <i class="fas fa-pen"></i>
<% end %>


<% @cocktail.reviews.each do |review| %>
  <p><%= "★" * review.rating %><%= "☆" * (5 - review.rating) %> - <%= review.content %></p>
<% end %>


