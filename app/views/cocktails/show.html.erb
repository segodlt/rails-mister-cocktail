<h1><%= @cocktail.name %> : </h1>


<% @cocktail.doses.each do |dose| %>
     <p><%= dose.description %> de : <%= dose.ingredient.name %> <%= link_to "🗑", dose_path(dose.id) %>  </p>
<% end %>

<%= link_to "Add an ingredient", new_cocktail_dose_path(@cocktail.id) %>
<%= link_to "Back", cocktails_path %>
